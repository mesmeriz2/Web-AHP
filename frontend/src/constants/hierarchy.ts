/** 계층 편집 시 부모 state로 반영하는 디바운스 지연(ms). 한글 IME 조합 중 리렌더 방지용 */
export const HIERARCHY_EDIT_DEBOUNCE_MS = 400;

/** 계층 노드 변경 후 자동 저장(API 호출)까지의 지연(ms) */
export const HIERARCHY_AUTO_SAVE_DELAY_MS = 500;
